# Momentum-Game-Theory -- pinned to versions confirmed working on this project (2026-08-03, Python 3.14).
# Install with: pip3 install -r requirements.txt

pandas==2.3.3
numpy==2.4.0
yfinance==1.4.1
requests==2.32.5
matplotlib==3.10.8

# Live execution only (strategy/live/) -- not needed to run backtest.py or signals.py.
ib_async==2.1.0

# Used only in connectivity_notebook.ipynb (Fama-French factor pull); not a dependency
# of the live strategy code.
pandas-datareader==0.11.1
